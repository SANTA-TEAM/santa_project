<nav class="col-12 col-md-auto fw-bold collapse navbar-collapse" id="navbarNav">
  <ul class="navbar-nav d-flex-md justify-content-start ps-0">
    <li class="nav-item">
      <a href="{{ path('app_home') }}" class="nav-link mx-1 py-2 px-4 text-uppercase
        {% if app.request.attributes.get('_route') is same as('app_home') %} text-secondary {% else %} text-primary {% endif %}">ACCUEIL
      </a>
    </li>
    <li class="nav-item">
      <a href="{{ path('app_gift') }}" class="nav-link mx-1 py-2 px-4 text-uppercase
        {% if app.request.attributes.get('_route') is same as('app_gift') %} text-secondary {% else %} text-primary {% endif %}">Salle aux Cadeaux
      </a>
    </li>
    <li class="nav-item">
      <a href="{{ path('app_letter') }}" class="nav-link mx-1 py-2 px-4 text-uppercase
        {% if app.request.attributes.get('_route') is same as('app_letter') %} text-secondary {% else %} text-primary {% endif %}">Lettre au Père Noël
      </a>
    </li>
    <li class="nav-item">
      <a href="{{ path('app_contact') }}" class="nav-link mx-1 py-2 px-4 text-uppercase
        {% if app.request.attributes.get('_route') is same as('app_contact') %} text-secondary {% else %} text-primary {% endif %}">CONTACT
      </a>
    </li>
  </ul>
</nav>